#include<bits/stdc++.h>


using namespace std;

int main() {
    int w, h, n;
    while(scanf("%d %d %d", &w, &h, &n)) {
        if (w == h == n == 0)
            break;
        vector<vector<bool>> board(h);



    }
    return 0;
}